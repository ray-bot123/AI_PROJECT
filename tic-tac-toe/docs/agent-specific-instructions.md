# Agent-Specific Instructions

Keep changes small and easy to review. Do not rewrite the game structure unless the requested change needs it.

Preserve the terminal-based interface unless a new UI is explicitly requested. When adding guidance, update the relevant `docs/` page and keep `AGENTS.md` linked to every guide file.
